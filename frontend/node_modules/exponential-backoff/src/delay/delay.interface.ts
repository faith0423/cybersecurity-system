export interface IDelay {
  apply: () => Promise<unknown>;
  setAttemptNumber: (attempt: number) => void;
}
export interface IDelay {
  apply: () => Promise<unknown>;
  setAttemptNumber: (attempt: number) => void;
}
