package com.example.cybersecurity.service;

import com.example.cybersecurity.model.Incident;
import com.example.cybersecurity.model.User;
import com.example.cybersecurity.repository.UserRepository;

import org.springframework.scheduling.annotation.Async;
import org.springframework.stereotype.Service;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.List;

@Service
public class NotificationEmailService {

    private final UserRepository userRepository;

    

    public NotificationEmailService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    @Async
    public void sendIncidentSolvedNotification(Incident incident, String resolvedBy) {
        System.out.println("=== EMAIL NOTIFICATION (disabled for deployment) ===");
        System.out.println("Would send email to admins for incident: " + incident.getId());
        System.out.println("Title: " + incident.getTitle());
        System.out.println("Resolved by: " + resolvedBy);
        System.out.println("==================================================");
    }

    public void sendIncidentReportPdf(byte[] pdfBytes, String generatedBy) {
        System.out.println("=== PDF REPORT EMAIL (disabled for deployment) ===");
        System.out.println("Would send PDF report email to: report recipients");
        System.out.println("Generated by: " + generatedBy);
        System.out.println("PDF size: " + (pdfBytes != null ? pdfBytes.length + " bytes" : "null"));
        System.out.println("==================================================");
    }
}